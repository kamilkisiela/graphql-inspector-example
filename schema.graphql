directive @log(message: String = "Default messages") on FIELD

type Post {
  id: ID
  title: String @deprecated(reason: "No more used")
  createdAt: String
  modifiedAt: Int
  removedAt: String
}

type Query {
  post: Post! @deprecated(reason: "No more used")
  posts: [Post!]
}
